import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Mapps = () => {
    return ( 
        <SafeAreaView>
              <Text>we ar safe</Text>
            </SafeAreaView>
     );
}
 
export default Mapps;